#creating code for data source

data "azurerm_resource_group" "datargdatails" {
  name = "rg-datasource"
}

resource "azurerm_virtual_network" "datavmdetails" {
  name                = "datavm"
  location            = data.azurerm_resource_group.datargdatails.location
  resource_group_name = data.azurerm_resource_group.datargdatails.name
  address_space       = ["10.0.0.0/16"]
  

  subnet {
    name             = "subnet1"
    address_prefixes = ["10.0.1.0/24"]
  }

  subnet {
    name             = "subnet2"
    address_prefixes = ["10.0.2.0/24"]

  }
}